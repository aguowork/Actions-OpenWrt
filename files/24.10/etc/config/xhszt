# 小红书状态监控配置
# 短链提供精确账号和最近作品数据；固定主页仅在结构完整时增强全部作品信息。
# 脚本会自动解析主页 ID 并写入 root 专属状态文件，后续用于防止短链跳转到错误账号。
# 固定主页返回 HTTP 200 空壳时会直接忽略，不会误报作品归零或全部删除。
# Base64 仅用于避免仓库直接展示分享短码，并不属于加密。
# 最近作品使用稳定封面资源路径识别，并排除可能轮换的 CDN 主机名和动态查询参数。
# 不使用每次请求都会变化的移动页面作品 ID。

config xhszt 'main'
	option enabled '0'
	option share_code_b64 'NUVkU1RudHB5UnA='
