#
# https://github.com/P3TERX/Actions-OpenWrt
# MT798x 系列路由器固件自动编译脚本

name: MT798x OpenWrt Build

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      device_selection:
        description: '选择要编译的设备'
        required: true
        type: choice
        options:
          - 全部
          - AX6000
          - WR30U
          - 360T7
        default: '全部'

env:
  REPO_URL: https://github.com/padavanonly/immortalwrt-mt798x.git
  REPO_BRANCH: openwrt-21.02
  FEEDS_CONF: feeds.conf.default
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 检出代码
      uses: actions/checkout@main

    - name: 初始化编译环境
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        # 清理系统空间
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        
        # 更新包列表
        sudo -E apt-get -qq update
        
        # 安装编译所需的依赖包
        sudo -E apt-get -qq install ack antlr3 asciidoc autoconf automake autopoint binutils bison build-essential bzip2 ccache cmake cpio curl device-tree-compiler fastjar flex gawk gettext gcc-multilib g++-multilib git gperf haveged help2man intltool libc6-dev-i386 libelf-dev libfuse-dev libglib2.0-dev libgmp3-dev libltdl-dev libmpc-dev libmpfr-dev libncurses5-dev libncursesw5-dev libpython3-dev libreadline-dev libssl-dev libtool lrzsz mkisofs msmtp ninja-build p7zip p7zip-full patch pkgconf python2.7 python3 python3-pyelftools python3-setuptools qemu-utils rsync scons squashfs-tools subversion swig texinfo uglifyjs upx-ucl unzip vim wget xmlto xxd zlib1g-dev
        
        # 清理 apt 缓存
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        
        # 设置时区
        sudo timedatectl set-timezone "$TZ"
        
        # 创建工作目录
        sudo mkdir -p /workdir
        sudo chown $USER:$GROUPS /workdir
        
        # 记录开始时间，并初始化 BUILD_RESULTS，作用是保存编译结果
        echo "START_TIME=$(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_ENV
        echo "BUILD_RESULTS=" >> $GITHUB_ENV

    - name: 克隆源码
      working-directory: /workdir
      run: |
        df -hT $PWD
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        ln -sf /workdir/openwrt $GITHUB_WORKSPACE/openwrt

    - name: 更新并安装软件源
      run: |
        cd openwrt
        ./scripts/feeds update -a
        ./scripts/feeds install -a

    - name: 编译固件
      id: build
      run: |
        if [ "${{ github.event.inputs.device_selection }}" = "全部" ]; then
          DEVICES=("AX6000" "WR30U" "360T7")
        else
          DEVICES=("${{ github.event.inputs.device_selection }}")
        fi
        
        echo "🔍 检测到设备: ${DEVICES[@]}"
        total_devices=${#DEVICES[@]}
        current_device=0
        
        # 使用JSON格式存储结果，便于后续处理
        results="["
        
        for device in "${DEVICES[@]}"; do
          ((current_device++))
          echo "📌 正在编译 [$current_device/$total_devices] ${device}..."
          device_start_time=$(date '+%Y-%m-%d %H:%M:%S')
          
          # 设置设备配置
          CONFIG_FILE="./config/21.02/immortalwrtARM/${device}/${device}-mtwifi-cfg.config"
          DIY_P1_SH="./diy/21.02/immortalwrtARM/${device}/diy-part1.sh"
          DIY_P2_SH="./diy/21.02/immortalwrtARM/${device}/diy-part2.sh"
          
          cd openwrt
          [ -e $CONFIG_FILE ] && cp $CONFIG_FILE .config
          [ -e $DIY_P1_SH ] && chmod +x $DIY_P1_SH && $GITHUB_WORKSPACE/$DIY_P1_SH
          [ -e $DIY_P2_SH ] && chmod +x $DIY_P2_SH && $GITHUB_WORKSPACE/$DIY_P2_SH
          
          make defconfig
          make download -j8
          echo "🔨 开始编译 ${device}..."
          make -j$(nproc) || make -j1 V=s
          
          if [ $? -eq 0 ]; then
            status="success"
            device_end_time=$(date '+%Y-%m-%d %H:%M:%S')
            firmware_path="bin/targets/*/${device}-firmware.bin"
            firmware_size=$([ -f $firmware_path ] && ls -lh $firmware_path | awk '{print $5}' || echo "未知")
            firmware_link="https://github.com/${{ github.repository }}/releases/download/latest/${device}-firmware.bin"
            echo "✅ ${device} 编译成功 (大小: ${firmware_size})"
          else
            status="failed"
            device_end_time=$(date '+%Y-%m-%d %H:%M:%S')
            firmware_size=""
            firmware_link=""
            echo "❌ ${device} 编译失败"
          fi
          
          # 添加JSON格式的编译结果
          [ "$current_device" -gt 1 ] && results="${results},"
          results="${results}{\"device\":\"${device}\",\"status\":\"${status}\",\"start_time\":\"${device_start_time}\",\"end_time\":\"${device_end_time}\",\"size\":\"${firmware_size}\",\"link\":\"${firmware_link}\"}"
          
          cd $GITHUB_WORKSPACE
        done
        
        results="${results}]"
        echo "BUILD_RESULTS<<EOF" >> $GITHUB_ENV
        echo "$results" >> $GITHUB_ENV
        echo "EOF" >> $GITHUB_ENV

    - name: 发送编译通知
      if: always()
      run: |
        if [ -z "${{ secrets.WXPUSHER_TOKEN }}" ] || [ -z "${{ secrets.WXPUSHER_UIDS }}" ]; then
          echo "⚠️ WXPusher配置未设置，跳过通知"
          exit 0
        fi
        
        notification="📦 OpenWrt固件编译完成\\n\\n"
        
        # 统计数据
        success_count=0
        fail_count=0
        
        # 解析JSON格式的编译结果
        echo "${{ env.BUILD_RESULTS }}" | jq -c '.[]' | while read -r item; do
          device=$(echo "$item" | jq -r '.device')
          status=$(echo "$item" | jq -r '.status')
          size=$(echo "$item" | jq -r '.size')
          link=$(echo "$item" | jq -r '.link')
          
          if [ "$status" = "success" ]; then
            ((success_count++))
            # 使用Markdown格式的链接，点击设备名称即可复制下载链接
            notification+="✅ [${device}](copy:${link}) (${size})\\n"
          else
            ((fail_count++))
            notification+="❌ ${device}\\n"
          fi
        done
        
        # 计算总耗时
        duration=$(( $(date +%s) - $(date -d "${{ env.START_TIME }}" +%s) ))
        hours=$((duration / 3600))
        minutes=$(( (duration % 3600) / 60 ))
        
        total=$((success_count + fail_count))
        notification+="\\n📊 统计: ${success_count}/${total} 成功"
        [ $fail_count -gt 0 ] && notification+=" (${fail_count} 失败)"
        notification+="\\n⏱️ 耗时: ${hours}h${minutes}m"
        
        # 发送通知
        curl -X POST \
          -H "Content-Type: application/json" \
          --retry 3 \
          -d "{
            \"appToken\": \"${{ secrets.WXPUSHER_TOKEN }}\",
            \"content\": \"$notification\",
            \"summary\": \"OpenWrt编译完成 [${success_count}/${total}]\",
            \"contentType\": 3,
            \"uids\": [\"${{ secrets.WXPUSHER_UIDS }}\"]
          }" \
          https://wxpusher.zjiecode.com/api/send/message || echo "❌ 通知发送失败"

    - name: 清理旧的发布
      uses: dev-drprasad/delete-older-releases@master
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 3
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: 清理工作流运行记录
      uses: Mattraks/delete-workflow-runs@main
      with:
        retain_days: 0
        keep_minimum_runs: 2 
